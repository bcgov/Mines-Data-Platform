# Local dev/test dependencies.
# pyspark/delta-spark pinned to the Fabric Runtime Spark version (Runtime 1.3 = Spark 3.5).
# NOTE: pyspark 3.5.x targets Python <=3.11; use a 3.11 venv for the local-Spark tests
# (silver/gold phases). Phase 1 path tests are pure-Python and need only pytest.
pyspark==3.5.1
delta-spark==3.2.0
pytest==8.2.0
